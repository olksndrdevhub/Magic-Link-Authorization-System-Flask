So this is Magic Link Authorization System.

For start you need create virtual environment, then clone this repo.  
`pip install > requirements.txt`  
`flask db init`  
`flask db migrate`  
`flask db upgrade`  
